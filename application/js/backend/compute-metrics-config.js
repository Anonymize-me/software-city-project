/**
 * @file computeMetricsConfig.js
 * @description This file contains the configuration for computing the metrics,
 * such as the excluded directories and the file extensions to consider.
 */
const excludedDirectories = ["node_modules", "dist"];

export { excludedDirectories };
