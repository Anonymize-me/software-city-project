const recalculateGlobalSum = (
   cityMetaphor,
   cityElements,
   modelTreeBuilder,
   sliderBuilder,
   guiBuilder,
   infoPanelBuilder
) => {
   /**
    * Implement logic here...
    */
};

export { recalculateGlobalSum };
