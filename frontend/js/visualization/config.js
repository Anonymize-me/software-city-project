/**
 * Configuration file for the visualization.
 */

const planeHeight = 0.25;

const paddingBuildings = 0.5;
const paddingPlanes = 0.4;

const minDimension = 0.25;
const maxDimension = 10;
const minHeight = 0.5;
const maxHeight = 25;
const minHue = 0;
const maxHue = 0.65;
const minLightness = 0.2;
const maxLightness = 0.5;

export {
   planeHeight,
   paddingBuildings,
   paddingPlanes,
   minDimension,
   maxDimension,
   minHeight,
   maxHeight,
   minHue,
   maxHue,
   minLightness,
   maxLightness,
};
